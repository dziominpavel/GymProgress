# GymProgress — Design System

> Дизайн-система для Android-приложения по трекингу силовых тренировок.
> Целевая аудитория: мужчины 25–45 лет. Стиль: минимализм, строгость, мотивация.

---

## 1. Цветовая палитра

### Философия
Тёмные нейтральные тона — основа. Оранжевый акцент — энергия и прогресс.
Никаких пастельных или «фитнес-йога» цветов. Контрастно, чётко, мужественно.

### Primary — Оранжевый (энергия, мотивация, сила)
| Токен | HEX | Назначение |
|-------|-----|------------|
| `Orange80` | `#FFB74D` | Primary на тёмном фоне |
| `Orange60` | `#FF9800` | Средний акцент |
| `Orange40` | `#E65100` | Primary на светлом фоне |
| `AccentOrange` | `#FF6D00` | CTA, FAB, progress bars |

### Secondary & Tertiary
| Токен | HEX | Назначение |
|-------|-----|------------|
| `OrangeGrey80/40` | `#D4C4A8` / `#5D4037` | Вторичные элементы |
| `Coral80/40` | `#FF8A65` / `#BF360C` | Третичные элементы |
| `AccentTeal` | `#00BFA5` | Успех, PR-бейджи |

### Semantic Colors
| Токен | HEX | Назначение |
|-------|-----|------------|
| `SuccessGreen` | `#4CAF50` | Новый рекорд, +прогресс |
| `WarningAmber` | `#FFC107` | Предупреждения |
| `ErrorRed` | `#EF5350` | Ошибки, удаление |

### Dark Theme Surfaces
| Токен | HEX | Назначение |
|-------|-----|------------|
| `DarkBackground` | `#0D0D0D` | Основной фон |
| `DarkSurface` | `#1A1A1A` | Карточки |
| `DarkSurfaceVariant` | `#242424` | Вторичные поверхности |
| `DarkSurfaceHigh` | `#2C2C2C` | Диалоги, sheets |

### Light Theme Surfaces
| Токен | HEX | Назначение |
|-------|-----|------------|
| `LightBackground` | `#F7F7F8` | Основной фон |
| `LightSurface` | `#FFFFFF` | Карточки |
| `LightSurfaceVariant` | `#F0F0F2` | Вторичные поверхности |
| `LightSurfaceHigh` | `#E8E8EA` | Нажатие / высокая элевация |

### Text Colors
| Токен | Dark | Light |
|-------|------|-------|
| Primary | `#F5F5F5` | `#121212` |
| Secondary | `#B0B0B0` | `#5F6368` |
| Disabled | `#616161` | `#9E9E9E` |

---

## 2. Типографика

Шрифт: **Roboto** (системный на Android).
Принцип: жирные, контрастные заголовки (Black/Bold) + спокойный body (Normal).

| Стиль | Вес | Размер | Высота строки | Tracking |
|-------|-----|--------|---------------|----------|
| Display Large | Black (900) | 57sp | 64sp | -0.25sp |
| Display Medium | Bold (700) | 45sp | 52sp | 0sp |
| Display Small | Bold (700) | 36sp | 44sp | 0sp |
| Headline Large | Bold (700) | 32sp | 40sp | 0sp |
| Headline Medium | Bold (700) | 28sp | 36sp | 0sp |
| Headline Small | SemiBold (600) | 24sp | 32sp | 0sp |
| Title Large | Bold (700) | 22sp | 28sp | 0sp |
| Title Medium | SemiBold (600) | 16sp | 24sp | 0.15sp |
| Title Small | SemiBold (600) | 14sp | 20sp | 0.1sp |
| Body Large | Normal (400) | 16sp | 24sp | 0.5sp |
| Body Medium | Normal (400) | 14sp | 20sp | 0.25sp |
| Body Small | Normal (400) | 12sp | 16sp | 0.4sp |
| Label Large | SemiBold (600) | 14sp | 20sp | 0.1sp |
| Label Medium | Medium (500) | 12sp | 16sp | 0.5sp |
| Label Small | Medium (500) | 11sp | 16sp | 0.5sp |

### Использование
- **Display** → hero-числа (макс. вес, PR)
- **Headline** → заголовки экранов
- **Title** → заголовки карточек, секций
- **Body** → основной контент
- **Label** → кнопки, чипы, бейджи, подписи

---

## 3. Сетка и отступы (Spacing System)

Base grid: **4dp**. Все значения кратны 4.

| Токен | Значение | Применение |
|-------|----------|------------|
| `xxxs` | 2dp | Микро-зазоры |
| `xxs` | 4dp | Между label и value |
| `xs` | 8dp | Между элементами в ряду |
| `sm` | 12dp | Внутри компонентов |
| `md` | 16dp | Основной padding |
| `lg` | 20dp | Между секциями |
| `xl` | 24dp | Крупные отступы |
| `xxl` | 32dp | Между блоками |
| `xxxl` | 40dp | Hero-секции |
| `huge` | 48dp | Верхний отступ экрана |
| `massive` | 64dp | Крупные разделители |

### Screen Padding
- Горизонтальный: **16dp**
- Вертикальный: **16dp**
- Нижний (для FAB): **80dp**

---

## 4. Формы компонентов (Shapes)

| Компонент | Скругление | Токен |
|-----------|-----------|-------|
| Кнопки | 12dp | `ButtonShape` |
| Карточки | 16dp | `CardShape` |
| Малые карточки | 12dp | `CardShapeSmall` |
| Диалоги | 20dp | `DialogShape` |
| Чипы / бейджи | 20dp | `ChipShape` |
| Поля ввода | 12dp | `TextFieldShape` |
| FAB | 16dp | `FabShape` |
| Bottom Sheet | 20dp top | `BottomSheetShape` |
| Изображения | 12dp | `ImageShape` |

### Material 3 Shapes
| Уровень | Значение |
|---------|----------|
| Small | 8dp |
| Medium | 12dp |
| Large | 16dp |
| Extra Large | 24dp |

**Принцип**: умеренное скругление. Не «детское» (24dp+ везде), не острое (0dp).
Строго, но мягко — как хорошо спроектированный тренажёр.

---

## 5. Стиль иконок

### Рекомендации
- **Основной стиль**: Material Icons **Outlined** — чистые линии, минимализм
- **Активные элементы**: Material Icons **Filled** — навигация, выбранные табы
- **Размеры**:
  - `iconXs` = 16dp (badge indicators)
  - `iconSm` = 20dp (inline icons)
  - `iconMd` = 24dp (стандарт, навигация)
  - `iconLg` = 32dp (hero-секции)
  - `iconXl` = 48dp (empty states)
- **Цвет иконок**: `onSurface` / `onSurfaceVariant`, акцентные — `primary`
- **Запрещено**: duotone, multicolor, 3D-иконки

---

## 6. Иллюстрации мышечных групп

Реализовано в `MuscleGroupIcon.kt` через Canvas API.

### Стиль
- **Силуэт тела** → нейтральный серый (`MuscleBodyGrey` #D6D6D6)
- **Выделенная группа мышц** → оранжевый (`MuscleHighlightPrimary` #EF6C00)
- **Световой акцент** → светло-оранжевый (`MuscleHighlightSecondary` #FF8F00)
- **Фон** → мягкий тёплый (#F5F0EB) — при необходимости `surface` из темы

### Размеры
| Вариант | Размер | Использование |
|---------|--------|---------------|
| Small | 40dp | Списки, dropdown |
| Default | 64dp | Карточки групп |
| Large | 80dp | Детальный просмотр |

### Принципы
- Геометричный силуэт, не анатомический атлас
- Highlight — только целевая группа мышц
- Одинаковые пропорции body для всех групп (единый `drawFullBody`)
- Скруглённый контейнер 12dp

---

## 7. Правила использования акцентного цвета

### ✅ Когда использовать `primary` / `AccentOrange`
- **FAB** (основное действие)
- **CTA-кнопки** (сохранить, создать)
- **Активный таб** навигации
- **Progress bars** и индикаторы прогресса
- **Accent bar** (вертикальная полоска у заголовков секций)
- **PR-бейджи** (личные рекорды)
- **Выделение максимального веса** в статистике

### ❌ Когда НЕ использовать
- Фон экранов или больших секций (перенасыщение)
- Основной текст (только для ссылок/кнопок)
- Одновременно более 2 акцентных элементов на экране
- Декоративные элементы без функциональной нагрузки

### Правило 60-30-10
- **60%** — нейтральные поверхности (`background`, `surface`)
- **30%** — вторичные тона (`surfaceVariant`, `outline`)
- **10%** — акцентный оранжевый

---

## 8. Состояния экранов

### Empty State
```kotlin
GymEmptyState(
    emoji = "🏋️",
    title = "Упражнений пока нет",
    subtitle = "Нажмите + чтобы создать первое",
    actionText = "Добавить",       // опционально
    onAction = { /* ... */ }       // опционально
)
```
- Emoji крупно (displayLarge)
- Заголовок (titleMedium, SemiBold, onSurfaceVariant)
- Подзаголовок (bodyMedium, onSurfaceVariant)
- Опциональная CTA-кнопка

### Loading State
```kotlin
GymLoadingState(message = "Загрузка...")
```
- CircularProgressIndicator (primary, 40dp, strokeWidth=3dp)
- Подпись (bodyMedium, onSurfaceVariant)

### Skeleton Loading
```kotlin
GymSkeletonList(count = 5)
```
- Shimmer-анимация (alpha 0.3→0.7, 800ms)
- Форма повторяет реальные карточки

### Error State
```kotlin
GymErrorState(
    message = "Не удалось загрузить данные",
    onRetry = { /* ... */ }
)
```
- Emoji ⚠️ (displayMedium)
- Заголовок (titleMedium, error color)
- Описание (bodyMedium)
- Кнопка «Повторить» (OutlinedButton)

---

## 9. Компоненты

### Кнопки
| Вариант | Composable | Стиль |
|---------|-----------|-------|
| Primary | `GymPrimaryButton` | Filled, primary, 48dp, 12dp radius |
| Outlined | `GymOutlinedButton` | Outlined, primary border, 48dp |
| Text | `GymTextButton` | Text only, primary color |

### Карточки
| Вариант | Composable | Стиль |
|---------|-----------|-------|
| Standard | `GymCard` | surfaceVariant, 16dp radius, padding 16dp |
| Elevated | `GymElevatedCard` | surface + elevation 2dp |

### Заголовки секций
```kotlin
GymSectionHeader(title = "История", subtitle = "последние записи")
```
- Вертикальная оранжевая полоска 4×20dp
- Title (titleMedium, Bold)
- Subtitle (bodySmall, onSurfaceVariant)

---

## 10. Extended Theme — `GymTheme`

Помимо стандартного `MaterialTheme`, доступен `GymTheme.colors`:

```kotlin
val accent = GymTheme.colors.accent           // AccentOrange
val success = GymTheme.colors.success          // зелёный (PR!)
val warning = GymTheme.colors.warning          // предупреждение
val divider = GymTheme.colors.divider          // разделитель
val disabled = GymTheme.colors.textDisabled    // неактивный текст
val highlight = GymTheme.colors.muscleHighlight // подсветка мышц
```

---

## 11. Файловая структура

```
ui/
├── theme/
│   ├── Color.kt        — цветовые токены
│   ├── Type.kt         — типографика
│   ├── Shape.kt        — формы компонентов
│   ├── Dimens.kt       — отступы, размеры, элевация
│   └── Theme.kt        — Material 3 тема + GymTheme
├── components/
│   ├── GymComponents.kt  — кнопки, карточки, состояния
│   └── MuscleGroupIcon.kt — иконки мышечных групп
└── screens/
    ├── ExercisesScreen.kt
    ├── JournalScreen.kt
    ├── StatsScreen.kt
    └── AddEntryDialog.kt
```

---

## 12. Elevation

| Уровень | Значение | Использование |
|---------|----------|---------------|
| None | 0dp | Flat cards (surfaceVariant) |
| Low | 1dp | Мягкое поднятие |
| Medium | 3dp | Стандартные карточки |
| High | 6dp | Диалоги |
| Overlay | 8dp | Bottom sheets, overlays |

---

## 13. Адаптивность

- Используется `NavigationSuiteScaffold` — автоматически переключается
  между bottom nav (телефон) и navigation rail (планшет)
- Все отступы через `Spacing` токены — легко масштабировать
- `LazyColumn` с `Arrangement.spacedBy(8.dp)` для списков

---

*Последнее обновление: февраль 2026*
